{
  "name": "@rbs/general-banks-info",
  "version": "1.1.9",
  "description": "Получение логотипа банка",
  "main": "lib/index.js",
  "scripts": {
    "test": "babel --presets es2015 -d spec_build/spec/ spec/ --copy-files && babel --presets es2015 -d spec_build/source/ source/ && cd spec_build && jasmine",
    "build:svg": "gulp svg",
    "build:main": "babel --presets es2015 -d lib/ source/ --copy-files",
    "copy-files": "cpx package.json lib",
    "build": "npm-run-all build:main build:svg copy-files"
  },
  "repository": {
    "type": "git"
  },
  "author": "",
  "license": "ISC",
  "babel": {
    "presets": [
      "babel-preset-es2015"
    ]
  },
  "devDependencies": {
    "babel-cli": "6.26.0",
    "babel-core": "6.26.3",
    "babel-preset-es2015": "6.24.1",
    "gulp": "4.0.2",
    "gulp-replace": "1.0.0",
    "gulp-svgmin": "2.2.0",
    "jasmine": "3.3.1",
    "npm-run-all": "^4.1.5"
  },
  "dependencies": {
    "cpx": "^1.5.0"
  }
}
